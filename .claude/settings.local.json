{
  "permissions": {
    "allow": [
      "Bash(rm -f /mnt/e/zk-agent/lib/middleware/file-upload-security.ts /mnt/e/zk-agent/lib/security/file-upload-validator.ts)",
      "Bash(rm -f /mnt/e/zk-agent/lib/cache/production-cache-manager.ts)",
      "Bash(sed -i '157i \\n    // 内存清理：限制慢查询数量\\n    if (this.slowQueries.length > 200) {\\n      this.slowQueries = this.slowQueries.slice(-100);\\n    }' /mnt/e/zk-agent/lib/database/query-optimizer.ts)",
      "Bash(sed -i '597i \\n    // 内存清理：缓存条目限制\\n    if (this.queryCache.size > 500) {\\n      const entries = Array.from(this.queryCache.entries())\\n        .sort(([,a],[,b]) => b.timestamp - a.timestamp)\\n        .slice(200);\\n      this.queryCache.clear();\\n      entries.forEach(([k,v]) => this.queryCache.set(k,v));\\n    }' /mnt/e/zk-agent/lib/database/query-optimizer.ts)",
      "Bash(sed -i '1a \\n/**\\n * @file 统一安全中间件 - 防御性优化版本\\n * @warning 仅用于安全分析，不暴露系统敏感操作\\n */' /mnt/e/zk-agent/lib/security/unified-security-validator.ts)",
      "Bash(npm run type-check --silent -- --noEmit)",
      "Bash(true)",
      "Bash(find /mnt/e/zk-agent/lib/database -name \"*.js\" -type f -delete)",
      "Bash(find /mnt/e/zk-agent -type f -name \"*.ts\" -exec grep -l \"production-database-manager\\|enhanced-connection\" {})",
      "Bash(ls:*)"
    ],
    "deny": []
  },
  "enabledMcpjsonServers": [
    "context7",
    "awesome-ionic-mcp"
  ]
}
# 强制使用精确版本，避免依赖冲突
save-exact=true

# 使用npm官方镜像
registry=https://registry.npmjs.org/

# 安全配置
audit-level=moderate
fund=false

# 性能优化
prefer-offline=true
cache-max=86400000

# 锁定文件配置
package-lock=true
shrinkwrap=false

# 日志级别
loglevel=warn

# 进度显示
progress=true